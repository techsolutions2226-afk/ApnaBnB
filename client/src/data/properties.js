/* ─── Shared property data with unique IDs ───
   Used by both the home page cards and the property detail page.
   When the backend is ready, replace this with API calls.
   ─────────────────────────────────────────────── */

import lahoreSection from "./properties/lahore";
import islamabadSection from "./properties/islamabad";
import karachiSection from "./properties/karachi";

const sections = [lahoreSection, islamabadSection, karachiSection];

export default sections;
